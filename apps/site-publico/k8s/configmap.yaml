apiVersion: v1
kind: ConfigMap
metadata:
  name: rsv-gen2-config
  namespace: rsv-gen2
data:
  NODE_ENV: "production"
  PORT: "3000"
  NEXT_TELEMETRY_DISABLED: "1"
  HOSTNAME: "0.0.0.0"
  # Adicionar outras variáveis não-sensíveis aqui

