# 🔧 CORREÇÃO DE DEPENDÊNCIAS

**Problema:** `@tanstack/react-query` não está instalado

## ✅ SOLUÇÃO

Execute no terminal:

```powershell
cd "D:\servidor RSV\Hotel-com-melhor-preco-main"
npm install @tanstack/react-query --legacy-peer-deps
```

Ou se preferir, execute:

```powershell
cd "D:\servidor RSV\Hotel-com-melhor-preco-main"
npm install
```

A dependência já foi adicionada ao `package.json`.

---

**Após instalar, reinicie o servidor Next.js:**
1. Pare o servidor (Ctrl+C)
2. Execute: `npm run dev`

